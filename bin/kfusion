#!/bin/bash

JARS=$(echo ${KFUSION_ROOT}/target/*.jar | tr ' ' ':')

JFLAGS="-Xms20G -Xmx26G -Dtornado.kernels.coarsener=False -Dlog4j.configurationFile=${KFUSION_ROOT}/conf/log4j2.xml"

CLASSPATH=${CLASSPATH}:${JARS} tornado ${JFLAGS} $@
